/**
 * BACKGROUND SERVICE WORKER — ENTRY POINT (WXT)
 *
 * Milestone 0 placeholder — full logic migrated in Milestone 3.
 * WXT handles service worker registration automatically.
 */
export default defineBackground(() => {
  console.log('🟢 LRI Background v0.20.0 loaded (WXT)');
});
